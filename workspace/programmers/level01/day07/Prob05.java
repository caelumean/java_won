package level01.day07;

public class Prob05 {
    public int[] solution(int[] arr) {
        int[] stk = {};
        return stk;
    }
    void main(){
        // 정수 배열 arr
        // stk
        // i는 0으로 설정 i가 arr의 길이보다 작으면
        // stk가 빈 배열이라면 arr[i] 를 stk에 추가하고 i에 1을 더한다
        // stk에 원소가 있고, 마지막 원소가 arr[i]보다 작으면
        // arr[i]를 stk의 뒤에 추가하고 i에 1을 더한다
        // stk에 원소가 있는데 마지막 원소가 arr[i] 보다 크거나 같으면
        // stk의 마지막 원소를 stk에서 제거한다.
    }
}
